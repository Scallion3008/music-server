[project]
name = "telegram-bot"
version = "0.1.0"
description = "Telegram bot for Ada's music server"
readme = "../README.md"
requires-python = ">=3.12"
dependencies = [
    "python-telegram-bot==22.6",
    "requests==2.32.5",
    "yt-dlp==2026.2.4",
]
